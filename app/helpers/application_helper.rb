module ApplicationHelper
  # 条件が true の場合のみ、divタグを表示する
  #def img_tag_if(condition, name,src = nil)
    # condition が trueの場合だけ、content_tagメソッドでdivタグを作成する
    # falseの場合は、何も作成しない
    #if condition
      #image_tag(condition,'src=<%= name.image %>', url_options)
    #end
  #end
end
